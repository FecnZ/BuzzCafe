// lib/theme/app_colors.dart
import 'package:flutter/material.dart';

class AppColors {
  /// Fondo principal (Crema claro)
  static const Color backgroundLight = Color(0xFFFAF8F5); 
  
  /// Acentos o contenedores secundarios (Tono latte)
  static const Color accentCoffee = Color(0xFFF3EBE1);
  
  /// Fondo para entradas de texto (TextFields)
  static const Color inputFill = Color(0xFFF9F6F0); 

  /// Color para botones o detalles principales
  static const Color primaryBrown = Color(0xFF5D4037); 
  
  /// Color para textos principales o títulos (Muy oscuro)
  static const Color textDark = Color(0xFF3E2723); 
  
  /// Color para íconos secundarios o bordes (Café medio)
  static const Color iconBrown = Color(0xFF8D6E63); 

  /// Color Naranja de acento (basado en el panel de administrador)
  static const Color primaryOrange = Color(0xFFF08C10);

  // ==========================================
  // PALETA PARA MODO OSCURO
  // ==========================================
  
  /// Fondo principal oscuro
  static const Color backgroundDark = Color(0xFF1B1B1B);
  
  /// Color de superficie (Tarjetas, diálogos)
  static const Color surfaceDark = Color(0xFF2C2C2C);
  
  /// Color primario para modo oscuro (un café un poco más vibrante)
  static const Color primaryCoffeeLight = Color(0xFFD7CCC8);
  
  /// Texto claro para fondo oscuro
  static const Color textLight = Color(0xFFEFEBE9);
}
